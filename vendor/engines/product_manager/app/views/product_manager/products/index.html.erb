<div class="row">
  <div class="span9">

    <div class="page-header">
      <h1>Listing products</h1>
    </div>

    <div class="well">
      <p class="help-block">Examine the url and note that the products page is namespaced within the Product Manager engine mounted at '/product_manager'. Also, note how the product objects themselves are namespaced under the Product Manager engine. This namespacing is reflected at the database level as well: the products table is called 'product_manager_products' by default.</p>
    </div>

    <table class="table table-bordered table-striped">
      <tr>
        <th>Product Object</th>
        <th>Name</th>
        <th>Price</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>

      <% @products.each do |product| %>
        <tr>
          <td><%= product %></td>
          <td><%= product.name %></td>
          <td><%= product.price %></td>
          <td><%= link_to 'Show', product %></td>
          <td><%= link_to 'Edit', edit_product_path(product) %></td>
          <td><%= link_to 'Destroy', product, confirm: 'Are you sure?', method: :delete %></td>
        </tr>
      <% end %>
    </table>

    <br />

    <%= link_to 'New Product', new_product_path, :class => 'btn btn-primary' %>
  </div>
</div>
